<div style="width: 360px; margin: 0 auto; padding: 60px 0; box-sizing: border-box; text-align: center;">
  <!-- 페이지 타이틀 -->
  <h1 style="font-size: 20px; font-weight: 700; margin-bottom: 16px;">
    비밀번호 재설정 인증번호 입력
  </h1>
  <!-- 안내 문구 -->
  <p style="font-size: 14px; color: #666; margin-bottom: 24px;">
    이메일로 받은 인증번호를 입력하세요.
  </p>

  <!-- 인증 폼 -->
  <%= form_with url: verify_password_token_path, method: :post, local: true do |f| %>
    <div style="max-width: 343px; margin: 0 auto; padding: 0 30px; box-sizing: border-box; text-align: left;">
      <!-- 인증번호 입력 필드 -->
      <div style="margin-bottom: 16px;">
        <label for="user_reset_password_token" style="display: block; font-size: 14px; font-weight: 600; margin-bottom: 6px;">
          인증번호
        </label>
        <%= f.text_field :reset_password_token, autofocus: true, required: true, placeholder: "인증번호 입력", style: "
              width: 100%;
              height: 44px;
              border: 1px solid #ccc;
              border-radius: 4px;
              padding: 0 10px;
              font-size: 14px;
              box-sizing: border-box;
            " %>
      </div>
      <!-- 인증번호 확인 버튼 -->
      <div style="margin-bottom: 16px;">
        <%= f.submit "인증번호 확인", style: "
              width: 100%;
              height: 44px;
              border: none;
              border-radius: 4px;
              background-color: #0AA5FF;
              color: #fff;
              font-size: 16px;
              font-weight: 600;
              cursor: pointer;
            " %>
      </div>
    </div>
  <% end %>

  <!-- 에러 메시지 -->
  <% if flash[:alert] %>
    <p style="font-size: 14px; color: red; margin-top: 12px;">
      <%= flash[:alert] %>
    </p>
  <% end %>
</div>
