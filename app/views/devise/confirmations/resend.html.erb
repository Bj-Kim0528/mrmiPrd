<h2>인증 이메일 재발송</h2>
<%= form_with url: resend_confirmation_path, method: :post, local: true do |f| %>
  <div class="field">
    <%= f.label :email, "이메일 주소" %>
    <%= f.email_field :email, required: true %>
  </div>
  <div class="actions">
    <%= f.submit "인증 이메일 재발송" %>
  </div>
<% end %>
